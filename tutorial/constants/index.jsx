const url = 'localhost'; // Change this to your server url, may help to run the ip.py file
const port = "8080";

const API = `http://${url}:${port}/api`; // localhost should actually be the IP address of your computer. This should technically be your public address. This is just an example.

export { API };